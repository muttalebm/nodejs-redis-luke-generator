{
  "name": "redis-luke-generator",
  "version": "1.0.0",
  "description": "",
  "main": "./dist/src/LukeGenerator.js",
  "types": "./dist/src/LukeGenerator.d.js",
  "scripts": {
    "setup": "npm install",
    "build": "tsc",
    "build-watch": "tsc --watch",
    "test-only": "mocha dist/test/",
    "test-only-tsc": "mocha -r ts-node/register 'test/*.ts'",
    "pretest": "npm run build",
    "test": "npm run test-only",
    "clean": "shx rm -rf node_modules/ dist/ docs/",
    "typedoc": "typedoc --out ./docs --mode modules --tsconfig ./tsconfig.json ./dev/src/",
    "posttypedoc": "shx cp .nojekyll docs/.nojekyll"
  },
  "author": "Abdul Muttaleb Maruf <ammaruf7@gmail.com>",
  "license": "ISC",
  "devDependencies": {
    "@types/chai": "^4.2.14",
    "@types/mocha": "^8.0.3",
    "chai": "^4.2.0",
    "mocha": "^8.2.0",
    "ts-node": "^9.0.0",
    "typedoc": "^0.19.2",
    "typescript": "^4.0.3",
    "rimraf": "^2.6.2",
    "shx": "^0.2.2"
  }
}
